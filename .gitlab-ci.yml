before_script:
    - pwd
    - git status
    - ssh-keyscan -t rsa gitlab.com >> ~/.ssh/known_hosts

stages:
    - test
    - deploy

deploy:
    stage: deploy
    only:
        - dev
    script:
        - git remote -v
        - git remote add staging root@104.236.115.202:/var/www/PayDonk.git 
        - git push staging dev
